diff --git arch/arm64/boot/dts/ti/k3-am62a7-sk.dts arch/arm64/boot/dts/ti/k3-am62a7-sk.dts
index a65a21e257..30a38dbe04 100644
--- arch/arm64/boot/dts/ti/k3-am62a7-sk.dts
+++ arch/arm64/boot/dts/ti/k3-am62a7-sk.dts
@@ -20,6 +20,7 @@
 		serial2 = &main_uart0;
 		mmc0 = &sdhci0;
 		mmc1 = &sdhci1;
+		mmc2 = &sdhci2;
 		ethernet0 = &cpsw_port1;
 	};
 
@@ -233,9 +234,57 @@
 			};
 		};
 	};
+
+	wlan_lten: regulator-5 {
+		compatible = "regulator-fixed";
+		regulator-name = "wlan_lten";
+		regulator-min-microvolt = <3300000>;
+		regulator-max-microvolt = <3300000>;
+		regulator-always-on;
+		enable-active-high;
+		vin-supply = <&vcc_3v3_sys>;
+		gpios = <&exp2 8 GPIO_ACTIVE_HIGH>;
+	};
+
+	wlan_en: regulator-6 {
+		/* OUTPUT of SN74AVC2T244DQMR */
+		compatible = "regulator-fixed";
+		regulator-name = "wlan_en";
+		regulator-min-microvolt = <1800000>;
+		regulator-max-microvolt = <1800000>;
+		enable-active-high;
+		vin-supply = <&wlan_lten>;
+		gpios = <&main_gpio0 71 GPIO_ACTIVE_HIGH>;
+		pinctrl-names = "default";
+		pinctrl-0 = <&wlan_en_pins_default>;
+	};
 };
 
 &main_pmx0 {
+	wlan_en_pins_default: wlan-en-pins-default {
+		pinctrl-single,pins = <
+			AM62AX_IOPAD(0x124, PIN_OUTPUT, 7) /* (A23) MMC2_SDCD.GPIO0_71 */
+		>;
+	};
+
+	main_mmc2_pins_default: main-mmc2-pins-default {
+		pinctrl-single,pins = <
+			AM62AX_IOPAD(0x120, PIN_INPUT, 0) /* (C24) MMC2_CMD */
+			AM62AX_IOPAD(0x118, PIN_INPUT, 0) /* (D25) MMC2_CLK */
+			AM62AX_IOPAD(0x114, PIN_INPUT, 0) /* (B24) MMC2_DAT0 */
+			AM62AX_IOPAD(0x110, PIN_INPUT, 0) /* (C25) MMC2_DAT1 */
+			AM62AX_IOPAD(0x10c, PIN_INPUT, 0) /* (E23) MMC2_DAT2 */
+			AM62AX_IOPAD(0x108, PIN_INPUT, 0) /* (D24) MMC2_DAT3 */
+			AM62AX_IOPAD(0x11c, PIN_INPUT, 0) /* (#N/A) MMC2_CLKB */
+		>;
+	};
+
+	main_wlirq_pins_default: main-wlirq-pins-default {
+		pinctrl-single,pins = <
+			AM62AX_IOPAD(0x128, PIN_INPUT, 7) /* (B23) MMC2_SDWP.GPIO0_72 */
+		>;
+	};
+
 	main_uart0_pins_default: main-uart0-pins-default {
 		pinctrl-single,pins = <
 			AM62AX_IOPAD(0x1c8, PIN_INPUT, 0) /* (D14) UART0_RXD */
@@ -518,6 +567,19 @@
 		reg = <0x23>;
 		gpio-controller;
 		#gpio-cells = <2>;
+		
+		gpio-line-names = "NC0", "NC1",
+				   "NC2", "NC3",
+				   "NC4", "NC5",
+				   "NC6", "NC7",
+				   "WL_LT_EN", "CSI_RSTz",
+				   "TP2", "TP5",
+				   "TP3", "TP6",
+				   "TP4", "TP7",
+				   "SPI0_FET_SEL", "SPI0_FET_OE",
+				   "GPIO_CPSW2_RST", "CSI_SEL2",
+				   "CSI_EN", "AUTO_100M_1000M_CONFIG",
+				   "CSI_VLDO_SEL", "SOC_WLAN_SDIO_RST";
 	};
 };
 
@@ -566,6 +628,33 @@
 	disable-wp;
 };
 
+&sdhci2 {
+	status = "okay";
+	vmmc-supply = <&wlan_en>;
+	pinctrl-names = "default";
+	pinctrl-0 = <&main_mmc2_pins_default>;
+	bus-width = <4>;
+	non-removable;
+	ti,fails-without-test-cd;
+	cap-power-off-card;
+	keep-power-in-suspend;
+	ti,driver-strength-ohm = <50>;
+	assigned-clocks = <&k3_clks 157 158>;
+	assigned-clock-parents = <&k3_clks 157 160>;
+
+	#address-cells = <1>;
+	#size-cells = <0>;
+	wlcore: wlcore@2 {
+		compatible = "ti,cc33xx";
+		reg = <2>;
+		pinctrl-names = "default";
+		pinctrl-0 = <&main_wlirq_pins_default>;
+		interrupt-parent = <&main_gpio0>;
+		interrupts = <72 IRQ_TYPE_EDGE_RISING>;
+		irq-gpio = <&main_gpio0 72 GPIO_ACTIVE_HIGH>;
+	};
+};
+
 &main_gpio0 {
 	status = "okay";
 };